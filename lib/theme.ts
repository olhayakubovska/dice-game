'use client';
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    secondary: {
      main: '#9C27B0',
      light: '#e1bee7',
    },
  },
});

export default theme;
